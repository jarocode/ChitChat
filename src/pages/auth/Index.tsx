import React from 'react';

import Layout from "layout";

interface AuthProps {
  
}

const Auth: React.FC<AuthProps> = ({  }) => {
    return (
        <Layout ><div></div></Layout>
    );
};

export default Auth;
