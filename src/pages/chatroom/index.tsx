import React from 'react';

import Layout from "layout";

interface AppProps {
    
}

const index: React.FC<AppProps> = ({  }) => {
    return (
        <Layout>
        <div>hello</div>
        </Layout>
    );
};

export default index;
